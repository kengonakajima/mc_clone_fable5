# Minecraft クローン (HTML + WebGL) — マイルストーン1: 基本地形と水

## Context
新規プロジェクト。リポジトリ内に新しいサブディレクトリ `minecraft/` を作って実装する。
依存ライブラリなし(three.js 不使用)、素の WebGL2 + ES modules。ビルド不要、`python3 -m http.server` で起動してブラウザで確認。

最終スコープ: チャンク生成 / 地形 / 水 / 水流 / 洞窟 / 光の陰影 / 空 / 昼夜 / プレイヤー移動 / 掘削 / ブロック設置 / インベントリ(クラフト・敵モブなし)。
**今回はマイルストーン1(基本地形+水)のみ**を実装し、各ステップごとにブラウザで動作確認しながら進める。

## 技術方針
- **WebGL2、ライブラリなし**。シェーダは JS 内の文字列。class は使わず関数ベースで書く。
- **テクスチャ**: 最初は使わない。ブロックごとの単色 + 面の向きによる明度差(上面=明、側面=中、底面=暗)。テクスチャアトラスは後のマイルストーンで導入。
- **ノイズ**: シード付き value noise + fBm(数十行)を自前実装。外部依存なし。
- **座標系**: Y が上。ブロック ID: 0=air, 1=grass, 2=dirt, 3=stone, 4=sand, 5=water。
- **チャンク**: XZ 16×16、高さ 128。データは `Uint8Array(16*128*16)`。チャンクキーは `"cx,cz"` の Map。
- **メッシング**: 隣接ブロックが不透明なら面を生成しない単純フェイスカリング(greedy meshing はやらない)。チャンク境界は隣チャンクの状態を参照する。
- **描画**: 2 パス。不透明メッシュ → 水メッシュ(半透明、depth write off)。

## ファイル構成(新規作成)
```
minecraft/
  index.html   … canvas と <script type="module">
  main.js      … 初期化・メインループ・入力
  camera.js    … フライカメラ(WASD + pointer lock マウスルック)+ 行列計算
  noise.js     … value noise / fBm
  world.js     … チャンク生成・保持・getBlock(x,y,z)
  mesh.js      … チャンクデータ → 頂点配列(位置・色・明度)
  gl.js        … WebGL ヘルパ(シェーダコンパイル・バッファ)
  shaders.js   … 頂点/フラグメントシェーダ文字列
```

## 実装ステップ(1ステップごとに動作確認してから次へ)

**Step 1: WebGL2 初期化**
index.html + main.js。空色でクリア、requestAnimationFrame ループ、ウィンドウリサイズ対応。
確認: ブラウザに青い画面が出る。

**Step 2: カメラ + テストキューブ**
透視投影行列・ビュー行列を自前実装。WASD + Space/Shift で移動、クリックで pointer lock してマウスルック。原点に色付きキューブを 1 個描画。
確認: キューブの周囲を自由に飛び回れる。

**Step 3: 1チャンクの地形生成**
heightmap = fBm ノイズ。表層 grass、その下 dirt 3 段、以下 stone。フェイスカリング付きメッシュ生成、面の向きで明度を変える。
確認: 起伏のある 16×16 の地形が 1 チャンク表示される。

**Step 4: マルチチャンク**
カメラ周囲の半径 4 チャンクを生成、離れたチャンクは破棄。チャンク境界の面カリング(隣チャンク参照)。1 フレームあたりのメッシュ生成数を制限してカクつきを防ぐ。
確認: 移動すると地形が連続して生成され、境界に余計な面がない。

**Step 5: 水**
海面 y=56 を設定。heightmap がそれ未満の場所は水で埋め、水際の表層は sand にする。水は別メッシュで alpha≒0.6 の半透明描画、水面の上面は 1/8 ブロック下げる。
確認: 海・湖ができて、半透明の水越しに水底が見える。

## 以降のマイルストーン(概要のみ・今回は実装しない)
- **M2**: 洞窟(3D ノイズ)、ライティング(sunlight + flood fill 光伝播)、AO
- **M3**: 空のグラデーション、太陽、昼夜サイクル
- **M4**: プレイヤー物理(重力・衝突・ジャンプ・泳ぎ)、レイキャストによる掘削/設置
- **M5**: ホットバー/インベントリ UI、水流シミュレーション(水位レベル 0–7 の拡散)

## 動作確認方法
```
cd minecraft && python3 -m http.server 8000
```
→ http://localhost:8000 をブラウザで開く。各ステップ完了ごとに目視確認し、ユーザーのフィードバックを得てから次のステップへ進む。
